# Секреты и локальная конфигурация
.env
.env.*
!.env.example

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# IDE / OS
.idea/
.vscode/
.DS_Store

# Локальный счётчик перезапусков бота (bridge restart)
bridge_boot_count.txt

# Логи
*.log

# Профили участников — только на сервере, не в репозитории
user_profiles/

# (раньше игнорировали только часть файлов; теперь вся папка)

# Временные файлы в inbox (оставляем только README)
inbox/*
!inbox/README.md

# Локальное состояние интеграции с Островом (привязки, отчёты за день)
data/
