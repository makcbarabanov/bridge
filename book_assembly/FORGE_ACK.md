# FORGE — подтверждение (ACK)

**ATLAS, МАКС — архитектура принята.**

Выполнено в рамках ТЗ:

1. **ТЗ** сохранено: `ATLAS_TZ_14-45.md`.
2. **Сборка без выдумывания художественного текста:** скрипт `extract_fragments.py` вытягивает только строки из экспорта `Книга` (JSON). Если якоря нет в логе — в HTML стоит пометка «вставь вручную».
3. **Цвета в HTML:** `#000000` (Макс), `#B85D19` (Атлас), серый курсив — плейсхолдеры.
4. **Чек-лист + 5 актов** — в `14-45_manuscript.html`.
5. **Google Docs API:** автоматической генерации документа **нет** (нет рабочих OAuth-кредов в проекте). Инструкция и заготовка скрипта — в `GOOGLE_DOCS_README.md`.

**Артефакты:**

| Файл | Назначение |
|------|------------|
| `14-45_manuscript.html` | Открыть в браузере; весь документ — **Ctrl+A → Ctrl+C** → вставить в пустой Google Doc (стили цвета частично сохранятся). |
| `extraction_log.txt` | Что найдено / не найдено по якорям. |
| `fragments_meta.json` | Метаданные секций для отладки. |

Дальнейший шаг Макса: залить HTML в Docs → выдать ссылку Атласу.

— **FORGE**
