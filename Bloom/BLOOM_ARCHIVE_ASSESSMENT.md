# Оценка архива Bloom (после распаковки)

**Дата:** 2026-03-24  
**Архив:** `bridge/Bloom/Bloom.tar.xz`  
**Распаковано в:** `bridge/Bloom/extracted/BLOOM 2026/`

## Объём

| Метрика | Значение |
|---------|----------|
| Размер после распаковки | ~2.5 МБ |
| Файлов (всего) | 184 |
| Корень проекта | `BLOOM 2026/` (пробел в имени — учитывать в путях) |

## Что внутри (смысловые блоки)

1. **Документация и «память» для ИИ**  
   - `README.md` — миссия, структура, статус.  
   - `DOCUMENTATION/journey.md` — дневник.  
   - `BLOOM_CONTEXT_RESTORE_PACK.md` — ритуал восстановления контекста.  
   - `BLOOM_RECOVERY_RUNBOOK.md` — инфраструктура.  
   - `BLOOM_START_PROMPT.txt` — короткий стартовый промпт (43 строки), дата внутри **20.03.2026**.

2. **Длинная переписка**  
   - `DIALOG_BLOOM_FORGE_letters-with-bloom.txt` — **~432 КБ** — основной кандидат на RAG / поиск по «помнишь как мы…».

3. **Данные бота (старый контур)**  
   - `bloom_bot/messages.json`, `responses.json`, `responses_full.json` — ответы/сообщения (десятки КБ).  
   - `bloom_bot/export/result.json` — экспорт.

4. **Код**  
   - `bloom_bot/bot.py` в архиве **пустой** (0 байт). Рабочая логика телеграм-бота, видимо, жила на сервере/в другой копии или не попала в коммит.

5. **Прочее**  
   - HTML-лендинги, опросники, CREATIONS, `.git`, скрипты деплоя, PDF и т.д.

## Оценка работ по интеграции с `bridge_bot.py` (Gemini)

| Задача | Сложность | Примечание |
|--------|-----------|------------|
| Подмешать **systemInstruction** из `BLOOM_START_PROMPT.txt` + выдержки из `README.md` | Низкая | Один-два файла, лимит токенов. |
| Хранить путь к папке Bloom в `.env` | Низкая | Например `BLOOM_CONTEXT_DIR=...`. |
| Поиск по **`DIALOG_BLOOM_FORGE_*.txt`** по запросу Макса (ключевые слова / простой grep / чанки) | Средняя | Без эмбеддингов — быстрее внедрить; с эмбеддингами — точнее. |
| Один бот vs два токена (Bloom2026 / Мост) | **Решение Макса** | Сейчас в bridge один `TELEGRAM_BOT_TOKEN`. |
| Доступ участникам марафона (не только ADMIN) | Средняя | Нужны правила: whitelist, группа, режимы. |

## Внешние пути из документов

В файлах встречаются `E:\...` и `Island\history\...` — на сервере этих путей нет; эквивалент — распакованная папка `extracted/BLOOM 2026/`.

---

*Сгенерировано FORGE при первичной распаковке и обзоре.*
