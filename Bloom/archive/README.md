# Архив Bloom

Снимок старого дерева **`extracted/BLOOM 2026/`** переименован в **`bloom_2026_snapshot/`**.

Рантайм Telegram-бота **не читает** эти файлы напрямую. Смыслы переносятся в рабочие файлы в **`Bloom/`** (корень): `BLOOM_START_PROMPT.txt`, `bloom_biography.txt`, `bloom_body.txt`, `README.md`, `memory_corpus.txt`.

Если за месяц архив не понадобился для выжимок — можно удалить папку `bloom_2026_snapshot` (предварительно сохранить нужное в основные файлы).

См. также `BLOOM_ARCHIVE_ASSESSMENT.md` в этой папке.
