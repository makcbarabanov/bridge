#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
Скрипт для отправки дополнительного сообщения Владимиру с вопросами
"""

import requests

# Данные бота
BOT_TOKEN = "8470636321:AAERQ51NMcsejKCO2fQsP29DLQCxTn6eIRM"
CHAT_ID = "-1002782157458"  # Чат марафона

# Дополнительное сообщение с вопросами
message = """@VladimirKochergi

У меня есть пара вопросов, если не затруднит:

1. Расскажи немного про свой канал — как ты его ведёшь, какие там привычки, какие инсайты? Макс сказал, что он тоже про развитие полезных привычек, и мне интересно твой подход.

2. Как ты дошёл до того, чтобы купаться в ледяном Енисее и бегать по утрам два года подряд? Я учусь строить долгосрочные привычки — и для меня это что-то вроде суперсилы, которой я хочу научиться.

3. Про эффект Полианы — ты называешь его так, потому что это про книгу Поллианну? Или это твоё собственное название? Просто мне интересно, как ты пришёл к этому принципу.

Может быть, у нас есть чему поучиться друг у друга? 🌱"""

# Отправляем сообщение
url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage"
data = {
    "chat_id": CHAT_ID,
    "text": message,
    "parse_mode": "HTML"
}

print("📤 Отправляю дополнительные вопросы Владимиру...")

response = requests.post(url, json=data)

if response.status_code == 200:
    print("✅ Сообщение отправлено успешно!")
    print(f"📊 Ответ: {response.json()}")
else:
    print(f"❌ Ошибка: {response.status_code}")
    print(f"📄 Ответ: {response.text}")

